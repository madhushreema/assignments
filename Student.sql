create  table tbl_student

(
sid int identity(100,1) primary key,
sname varchar(100),
scity varchar(100),
sadd varchar(100),
semail varchar(100)
)
select * from tbl_student
create database ku
use ku